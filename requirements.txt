fastapi>=0.109.0
uvicorn[standard]>=0.27.0
prophet>=1.1.5
pandas>=2.1.0
numpy>=1.26.0
psycopg2-binary>=2.9.9
redis>=5.0.0
pydantic>=2.5.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0
pytest>=8.0.0
httpx>=0.26.0

# OpenTelemetry distributed tracing (Phase 1: traces only, exported via OTLP/HTTP).
# Keep the opentelemetry-* versions in lockstep to avoid API/SDK mismatches.
opentelemetry-sdk>=1.27.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.27.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.48b0,<1.0.0
opentelemetry-instrumentation-psycopg2>=0.48b0,<1.0.0

# Prometheus metrics (pull model via /metrics) — the metrics half of the
# observability split (traces go through OpenTelemetry/OTLP above). Mirrors the
# api-server's micrometer-registry-prometheus actuator endpoint.
prometheus-client>=0.20.0,<1.0.0
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0
